name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy-test-server:
    name: deploy backend
    runs-on: ubuntu-latest
    steps:
      - name: build
        uses: gappleboy/ssh-action@master && sh deploy.sh
        with:
          host: ${{ secrets.HOST}}
          username: root
          port: ${{ secrets.PORT}}
          password: ${{ secrets.PASSWORD}}
          command: cd /var/www/beesquad && sh deploy.sh
